<Window x:Class="FlipPix.UI.VideoEnhanceWindow"
        Icon="flippix.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FlipPix - Enhance Video" Height="800" Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        MinHeight="650" MinWidth="800"
        ResizeMode="CanResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" MouseLeftButtonDown="Header_MouseLeftButtonDown" Cursor="SizeAll">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1565C0" Offset="0"/>
                    <GradientStop Color="#42A5F5" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Margin="25,20">
                <TextBlock Text="Enhance Video"
                           FontSize="28" FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock Text="Interpolate frames for smooth slow-motion  •  Upscale resolution with RTX Super Resolution"
                           FontSize="13" Foreground="#E3F2FD"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20,15,20,15">

                <TabControl>

                    <!-- ── Tab 1: Interpolate ── -->
                    <TabItem Header="🎞 Interpolate">
                        <StackPanel Margin="10,15,10,10">

                            <!-- Input Section -->
                            <Border Style="{StaticResource SectionPanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Input Video"
                                               FontSize="14" FontWeight="Bold"
                                               Foreground="#1565C0" Margin="0,0,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0"
                                                 Text="{Binding InterpolateVideoPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 IsReadOnly="True"
                                                 Padding="8,6"
                                                 FontSize="12"
                                                 Background="White"
                                                 BorderBrush="#CED4DA"
                                                 VerticalContentAlignment="Center"
                                                 Height="34"/>
                                        <Button Grid.Column="1"
                                                Content="Browse"
                                                Style="{StaticResource PrimaryButtonStyle}"
                                                Command="{Binding SelectInterpolateVideoCommand}"
                                                Width="80" Height="34"
                                                Margin="8,0,0,0"/>
                                    </Grid>

                                    <TextBlock Text="{Binding InterpolateVideoInfo}"
                                               FontSize="11" Foreground="#6C757D"
                                               Margin="0,6,0,0"
                                               Visibility="{Binding HasInterpolateVideo, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <Button Content="Add to Queue"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Command="{Binding AddInterpolateToQueueCommand}"
                                            HorizontalAlignment="Left"
                                            Width="140" Height="36"
                                            Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Queue Section -->
                            <Border Style="{StaticResource SectionPanelStyle}"
                                    Visibility="{Binding HasInterpolateQueueItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="Queue"
                                                   FontSize="14" FontWeight="Bold"
                                                   Foreground="#1565C0"/>
                                        <TextBlock Text="{Binding InterpolateQueueStatus}"
                                                   FontSize="11" Foreground="#6C757D"
                                                   VerticalAlignment="Center"
                                                   Margin="10,0,0,0"/>
                                    </StackPanel>

                                    <ItemsControl ItemsSource="{Binding InterpolateQueue}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="White"
                                                        BorderBrush="#DEE2E6"
                                                        BorderThickness="1"
                                                        CornerRadius="5"
                                                        Padding="10,8"
                                                        Margin="0,0,0,6">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding DisplayText}"
                                                                       FontSize="12" FontWeight="SemiBold"
                                                                       Foreground="#212529"/>
                                                            <TextBlock Text="{Binding StatusDisplay}"
                                                                       FontSize="11"
                                                                       Foreground="{Binding StatusColor}"
                                                                       Margin="0,2,0,0"/>
                                                            <TextBlock Text="{Binding ErrorMessage}"
                                                                       FontSize="10" Foreground="#DC3545"
                                                                       Margin="0,2,0,0"
                                                                       Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                        </StackPanel>

                                                        <ProgressBar Grid.Column="1"
                                                                     Value="{Binding Progress}"
                                                                     Minimum="0" Maximum="100"
                                                                     Width="80" Height="6"
                                                                     Margin="10,0,10,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#1565C0"/>

                                                        <Button Grid.Column="2"
                                                                Content="✕"
                                                                Width="24" Height="24"
                                                                FontSize="10"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Foreground="#6C757D"
                                                                Cursor="Hand"
                                                                Command="{Binding DataContext.RemoveInterpolateQueueItemCommand,
                                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </TabItem>

                    <!-- ── Tab 2: Upscale ── -->
                    <TabItem Header="🔍 Upscale">
                        <StackPanel Margin="10,15,10,10">

                            <!-- Input Section -->
                            <Border Style="{StaticResource SectionPanelStyle}">
                                <StackPanel>
                                    <TextBlock Text="Input Video"
                                               FontSize="14" FontWeight="Bold"
                                               Foreground="#1565C0" Margin="0,0,0,10"/>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox Grid.Column="0"
                                                 Text="{Binding UpscaleVideoPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                 IsReadOnly="True"
                                                 Padding="8,6"
                                                 FontSize="12"
                                                 Background="White"
                                                 BorderBrush="#CED4DA"
                                                 VerticalContentAlignment="Center"
                                                 Height="34"/>
                                        <Button Grid.Column="1"
                                                Content="Browse"
                                                Style="{StaticResource PrimaryButtonStyle}"
                                                Command="{Binding SelectUpscaleVideoCommand}"
                                                Width="80" Height="34"
                                                Margin="8,0,0,0"/>
                                    </Grid>

                                    <TextBlock Text="{Binding UpscaleVideoInfo}"
                                               FontSize="11" Foreground="#6C757D"
                                               Margin="0,6,0,0"
                                               Visibility="{Binding HasUpscaleVideo, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                    <Button Content="Add to Queue"
                                            Style="{StaticResource PrimaryButtonStyle}"
                                            Command="{Binding AddUpscaleToQueueCommand}"
                                            HorizontalAlignment="Left"
                                            Width="140" Height="36"
                                            Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Queue Section -->
                            <Border Style="{StaticResource SectionPanelStyle}"
                                    Visibility="{Binding HasUpscaleQueueItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="Queue"
                                                   FontSize="14" FontWeight="Bold"
                                                   Foreground="#1565C0"/>
                                        <TextBlock Text="{Binding UpscaleQueueStatus}"
                                                   FontSize="11" Foreground="#6C757D"
                                                   VerticalAlignment="Center"
                                                   Margin="10,0,0,0"/>
                                    </StackPanel>

                                    <ItemsControl ItemsSource="{Binding UpscaleQueue}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="White"
                                                        BorderBrush="#DEE2E6"
                                                        BorderThickness="1"
                                                        CornerRadius="5"
                                                        Padding="10,8"
                                                        Margin="0,0,0,6">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding DisplayText}"
                                                                       FontSize="12" FontWeight="SemiBold"
                                                                       Foreground="#212529"/>
                                                            <TextBlock Text="{Binding StatusDisplay}"
                                                                       FontSize="11"
                                                                       Foreground="{Binding StatusColor}"
                                                                       Margin="0,2,0,0"/>
                                                            <TextBlock Text="{Binding ErrorMessage}"
                                                                       FontSize="10" Foreground="#DC3545"
                                                                       Margin="0,2,0,0"
                                                                       Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                        </StackPanel>

                                                        <ProgressBar Grid.Column="1"
                                                                     Value="{Binding Progress}"
                                                                     Minimum="0" Maximum="100"
                                                                     Width="80" Height="6"
                                                                     Margin="10,0,10,0"
                                                                     VerticalAlignment="Center"
                                                                     Foreground="#1565C0"/>

                                                        <Button Grid.Column="2"
                                                                Content="✕"
                                                                Width="24" Height="24"
                                                                FontSize="10"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                Foreground="#6C757D"
                                                                Cursor="Hand"
                                                                Command="{Binding DataContext.RemoveUpscaleQueueItemCommand,
                                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </TabItem>

                </TabControl>

                <!-- Progress Section -->
                <Border Style="{StaticResource SectionPanelStyle}"
                        Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Processing"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="#1565C0" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding ProcessingStatus}"
                                   FontSize="12" Foreground="#495057"
                                   Margin="0,0,0,6"/>
                        <ProgressBar Value="{Binding ProcessingProgress}"
                                     Minimum="0" Maximum="100"
                                     Height="10"
                                     Foreground="#1565C0"/>
                        <TextBlock Text="{Binding ProgressPercentage}"
                                   FontSize="11" Foreground="#6C757D"
                                   HorizontalAlignment="Right"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Result Section -->
                <Border Style="{StaticResource SectionPanelStyle}"
                        Visibility="{Binding HasResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Result"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="#28A745" Margin="0,0,0,8"/>

                        <MediaElement x:Name="VideoPlayer"
                                      Source="{Binding ResultVideoPath}"
                                      LoadedBehavior="Manual"
                                      UnloadedBehavior="Stop"
                                      Stretch="Uniform"
                                      MaxHeight="300"
                                      Margin="0,0,0,10"/>

                        <TextBlock Text="{Binding ResultVideoInfo}"
                                   FontSize="11" Foreground="#6C757D"
                                   Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal">
                            <Button Content="Play"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding PlayVideoCommand}"
                                    Width="80" Height="32"
                                    Margin="0,0,8,0"/>
                            <Button Content="Open Folder"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Command="{Binding OpenResultFolderCommand}"
                                    Width="100" Height="32"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Log Section -->
                <Border Style="{StaticResource SectionPanelStyle}">
                    <StackPanel>
                        <TextBlock Text="Log"
                                   FontSize="13" FontWeight="Bold"
                                   Foreground="#495057" Margin="0,0,0,6"/>
                        <ScrollViewer Height="120"
                                      VerticalScrollBarVisibility="Auto"
                                      Background="White"
                                      BorderBrush="#DEE2E6"
                                      BorderThickness="1">
                            <TextBlock Text="{Binding LogOutput}"
                                       FontFamily="Consolas"
                                       FontSize="10"
                                       Foreground="#495057"
                                       Padding="8"
                                       TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" Padding="15,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding ProcessingStatus}"
                           FontSize="11" Foreground="#495057"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Interpolate: "
                               FontSize="11" Foreground="#6C757D"
                               VerticalAlignment="Center"/>
                    <TextBlock FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Idle"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProcessingInterpolateQueue}" Value="True">
                                        <Setter Property="Text" Value="Running"/>
                                        <Setter Property="Foreground" Value="#FF6B35"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="Upscale: "
                               FontSize="11" Foreground="#6C757D"
                               VerticalAlignment="Center"/>
                    <TextBlock FontSize="11" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Idle"/>
                                <Setter Property="Foreground" Value="#6C757D"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProcessingUpscaleQueue}" Value="True">
                                        <Setter Property="Text" Value="Running"/>
                                        <Setter Property="Foreground" Value="#FF6B35"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>
